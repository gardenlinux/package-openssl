:100644 100644 e2083fa76b 0000000000 M	apps/rehash.c

diff --git a/apps/rehash.c b/apps/rehash.c
index e2083fa76b..c1835e2191 100644
--- a/apps/rehash.c
+++ b/apps/rehash.c
@@ -305,3 +305,3 @@ static int do_file(const char *filename, const char *fullpath, enum Hash h)
             } else {
-                BIO_printf(bio_err, "%s: error calculating SHA1 hash value\n",
+                BIO_printf(bio_err, "%s: error calculating SHA256 hash value\n",
                     opt_getprog());
@@ -555,3 +555,3 @@ int rehash_main(int argc, char **argv)
 
-    evpmd = EVP_sha1();
+    evpmd = EVP_sha256();
     evpmdsize = EVP_MD_get_size(evpmd);

:100644 100644 fa085edeb8 0000000000 M	include/crypto/x509.h

diff --git a/include/crypto/x509.h b/include/crypto/x509.h
index fa085edeb8..788acf2109 100644
--- a/include/crypto/x509.h
+++ b/include/crypto/x509.h
@@ -119,3 +119,3 @@ struct X509_crl_st {
     /* hash of CRL */
-    unsigned char sha1_hash[SHA_DIGEST_LENGTH];
+    unsigned char sha256_hash[SHA_DIGEST_LENGTH];
     /* alternative method to handle this CRL */
@@ -197,3 +197,3 @@ struct x509_st {
 #endif
-    unsigned char sha1_hash[SHA_DIGEST_LENGTH];
+    unsigned char sha256_hash[SHA_DIGEST_LENGTH];
     X509_CERT_AUX *aux;
:100644 100644 3c1bdd84a7 0000000000 M	crypto/x509/v3_purp.c

diff --git a/crypto/x509/v3_purp.c b/crypto/x509/v3_purp.c
index 3c1bdd84a7..3f8ed83332 100644
--- a/crypto/x509/v3_purp.c
+++ b/crypto/x509/v3_purp.c
@@ -465,3 +465,3 @@ int ossl_x509v3_cache_extensions(X509 *x)
     /* Cache the SHA1 digest of the cert */
-    if (!X509_digest(x, EVP_sha1(), x->sha1_hash, NULL))
+    if (!X509_digest(x, EVP_sha256(), x->sha256_hash, NULL))
         x->ex_flags |= EXFLAG_NO_FINGERPRINT;
:100644 100644 1de8274c28 0000000000 M	crypto/x509/x_all.c

diff --git a/crypto/x509/x_all.c b/crypto/x509/x_all.c
index 1de8274c28..ef858be64c 100644
--- a/crypto/x509/x_all.c
+++ b/crypto/x509/x_all.c
@@ -500,8 +500,8 @@ int X509_digest(const X509 *cert, const EVP_MD *md, unsigned char *data,
 {
-    if (EVP_MD_is_a(md, SN_sha1) && (cert->ex_flags & EXFLAG_SET) != 0
+    if (EVP_MD_is_a(md, SN_sha256) && (cert->ex_flags & EXFLAG_SET) != 0
         && (cert->ex_flags & EXFLAG_NO_FINGERPRINT) == 0) {
-        /* Asking for SHA1 and we already computed it. */
+        /* Asking for SHA256 and we already computed it. */
         if (len != NULL)
-            *len = sizeof(cert->sha1_hash);
-        memcpy(data, cert->sha1_hash, sizeof(cert->sha1_hash));
+            *len = sizeof(cert->sha256_hash);
+        memcpy(data, cert->sha256_hash, sizeof(cert->sha256_hash));
         return 1;
@@ -614,9 +614,9 @@ int X509_CRL_digest(const X509_CRL *data, const EVP_MD *type,
     }
-    if (EVP_MD_is_a(type, SN_sha1)
+    if (EVP_MD_is_a(type, SN_sha256)
         && (data->flags & EXFLAG_SET) != 0
         && (data->flags & EXFLAG_NO_FINGERPRINT) == 0) {
-        /* Asking for SHA1; always computed in CRL d2i. */
+        /* Asking for SHA256; always computed in CRL d2i. */
         if (len != NULL)
-            *len = sizeof(data->sha1_hash);
-        memcpy(md, data->sha1_hash, sizeof(data->sha1_hash));
+            *len = sizeof(data->sha256_hash);
+        memcpy(md, data->sha256_hash, sizeof(data->sha256_hash));
         return 1;
:100644 100644 4a93abb8ca 0000000000 M	crypto/x509/x_crl.c

diff --git a/crypto/x509/x_crl.c b/crypto/x509/x_crl.c
index 4a93abb8ca..7133c5c390 100644
--- a/crypto/x509/x_crl.c
+++ b/crypto/x509/x_crl.c
@@ -189,3 +189,3 @@ static int crl_cb(int operation, ASN1_VALUE **pval, const ASN1_ITEM *it,
     case ASN1_OP_D2I_POST:
-        if (!X509_CRL_digest(crl, EVP_sha1(), crl->sha1_hash, NULL))
+        if (!X509_CRL_digest(crl, EVP_sha256(), crl->sha256_hash, NULL))
             crl->flags |= EXFLAG_NO_FINGERPRINT;
:100644 100644 6d9848d234 0000000000 M	crypto/x509/x509_cmp.c

diff --git a/crypto/x509/x509_cmp.c b/crypto/x509/x509_cmp.c
index 6d9848d234..6dcf455b18 100644
--- a/crypto/x509/x509_cmp.c
+++ b/crypto/x509/x509_cmp.c
@@ -92,3 +92,3 @@ int X509_CRL_match(const X509_CRL *a, const X509_CRL *b)
         && (b->flags & EXFLAG_NO_FINGERPRINT) == 0)
-        rv = memcmp(a->sha1_hash, b->sha1_hash, SHA_DIGEST_LENGTH);
+        rv = memcmp(a->sha256_hash, b->sha256_hash, SHA256_DIGEST_LENGTH);
     else
@@ -164,3 +164,3 @@ int X509_cmp(const X509 *a, const X509 *b)
         && (b->ex_flags & EXFLAG_NO_FINGERPRINT) == 0)
-        rv = memcmp(a->sha1_hash, b->sha1_hash, SHA_DIGEST_LENGTH);
+        rv = memcmp(a->sha256_hash, b->sha256_hash, SHA256_DIGEST_LENGTH);
     if (rv != 0)
@@ -293,4 +293,4 @@ unsigned long X509_NAME_hash_ex(const X509_NAME *x, OSSL_LIB_CTX *libctx,
     unsigned long ret = 0;
-    unsigned char md[SHA_DIGEST_LENGTH];
-    EVP_MD *sha1 = EVP_MD_fetch(libctx, "SHA1", propq);
+    unsigned char md[SHA256_DIGEST_LENGTH];
+    EVP_MD *sha256 = EVP_MD_fetch(libctx, "SHA256", propq);
     int i2d_ret;
@@ -301,4 +301,4 @@ unsigned long X509_NAME_hash_ex(const X509_NAME *x, OSSL_LIB_CTX *libctx,
         *ok = 0;
-    if (i2d_ret >= 0 && sha1 != NULL
-        && EVP_Digest(x->canon_enc, x->canon_enclen, md, NULL, sha1, NULL)) {
+    if (i2d_ret >= 0 && sha256 != NULL
+        && EVP_Digest(x->canon_enc, x->canon_enclen, md, NULL, sha256, NULL)) {
         ret = (((unsigned long)md[0]) | ((unsigned long)md[1] << 8L) | ((unsigned long)md[2] << 16L) | ((unsigned long)md[3] << 24L)) & 0xffffffffL;
@@ -307,3 +307,3 @@ unsigned long X509_NAME_hash_ex(const X509_NAME *x, OSSL_LIB_CTX *libctx,
     }
-    EVP_MD_free(sha1);
+    EVP_MD_free(sha256);
     return ret;
:100644 100644 ca27520183 0000000000 M	crypto/x509/t_x509.c

diff --git a/crypto/x509/t_x509.c b/crypto/x509/t_x509.c
index ca27520183..ca7f794525 100644
--- a/crypto/x509/t_x509.c
+++ b/crypto/x509/t_x509.c
@@ -202,3 +202,3 @@ int X509_ocspid_print(BIO *bp, X509 *x)
     int i;
-    unsigned char SHA1md[SHA_DIGEST_LENGTH];
+    unsigned char SHA256md[SHA256_DIGEST_LENGTH];
     ASN1_BIT_STRING *keybstr;
@@ -223,9 +223,9 @@ int X509_ocspid_print(BIO *bp, X509 *x)
 
-    md = EVP_MD_fetch(x->libctx, SN_sha1, x->propq);
+    md = EVP_MD_fetch(x->libctx, SN_sha256, x->propq);
     if (md == NULL)
         goto err;
-    if (!EVP_Digest(der, derlen, SHA1md, NULL, md, NULL))
+    if (!EVP_Digest(der, derlen, SHA256md, NULL, md, NULL))
         goto err;
-    for (i = 0; i < SHA_DIGEST_LENGTH; i++) {
-        if (BIO_printf(bp, "%02X", SHA1md[i]) <= 0)
+    for (i = 0; i < SHA256_DIGEST_LENGTH; i++) {
+        if (BIO_printf(bp, "%02X", SHA256md[i]) <= 0)
             goto err;
@@ -247,6 +247,6 @@ int X509_ocspid_print(BIO *bp, X509 *x)
     if (!EVP_Digest(ASN1_STRING_get0_data(keybstr),
-            ASN1_STRING_length(keybstr), SHA1md, NULL, md, NULL))
+            ASN1_STRING_length(keybstr), SHA256md, NULL, md, NULL))
         goto err;
-    for (i = 0; i < SHA_DIGEST_LENGTH; i++) {
-        if (BIO_printf(bp, "%02X", SHA1md[i]) <= 0)
+    for (i = 0; i < SHA256_DIGEST_LENGTH; i++) {
+        if (BIO_printf(bp, "%02X", SHA256md[i]) <= 0)
             goto err;
:100644 100644 469ff70e80 0000000000 M	crypto/x509/v3_skid.c

diff --git a/crypto/x509/v3_skid.c b/crypto/x509/v3_skid.c
index 469ff70e80..45f37bb82b 100644
--- a/crypto/x509/v3_skid.c
+++ b/crypto/x509/v3_skid.c
@@ -70,3 +70,3 @@ ASN1_OCTET_STRING *ossl_x509_pubkey_hash(X509_PUBKEY *pubkey)
         return NULL;
-    if ((md = EVP_MD_fetch(libctx, SN_sha1, propq)) == NULL)
+    if ((md = EVP_MD_fetch(libctx, SN_sha256, propq)) == NULL)
         return NULL;
